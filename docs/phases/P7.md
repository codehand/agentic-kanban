# P7 — JSON read API + Web UI wiring + SSE

> Nguồn: `IMPLEMENTATION_PLAN.md` §5 (P7), §9 (wiring UI tại chỗ) · `TASK_HUB_DESIGN.md` §9 (human review
> + web UI), §6 (`task.approve`), §8 (timeline) · `UI_DESIGN_BRIEF.md` (8 màn) · `design-system/`.

## Mục tiêu

Biến prototype `design-system/` thành **app thật, dữ liệu sống**: read JSON API + SSE + write-action cho
human, thay mock data bằng fetch. Một human operator review/approve qua UI.

## Phạm vi

In scope:
- Read endpoints: `GET /api/projects`, `/api/tasks?project&state`, `/api/tasks/:key` (spec + gitref +
  evidence summary + timeline), `/api/evidence/:key`, `/api/tokens`.
- Write cho human (bearer human): approve / reset / remove.
- **SSE** `/api/stream` đẩy update (transition, lease tick); poll là fallback (`IMPLEMENTATION_PLAN.md` §3).
- Token gate (màn S5 sign-in): nhận token, lưu trên device; deep-link `/p/<project>`, `/t/<KEY>`.
- Wiring UI **tại chỗ** trong `design-system/` (HTML+JS): thay mock data bằng fetch + SSE; dùng lại
  loading/empty/error đã có trong prototype.

Out of scope: drag-to-change-state, agent chat/console, full diff viewer, auto-merge MR (`IMPLEMENTATION_PLAN.md` §10).

## Module & file

- `server/src/api/routes.ts` — read endpoints + write human actions (trên `node:http`).
- `server/src/api/stream.ts` — SSE `/api/stream`.
- `server/src/http/static.ts` — serve `design-system/` (web/).
- `design-system/*.js` — thay mock data bằng fetch + SSE; token store; deep-link.

## Task checklist

- [ ] Read endpoints projects/tasks/task detail/evidence/tokens.
- [ ] Write human actions approve/reset/remove (bearer human).
- [ ] SSE `/api/stream` phát transition + lease tick.
- [ ] Token gate (S5) + deep-link `/p/<project>`, `/t/<KEY>`.
- [ ] Wiring UI tại chỗ: fetch thay mock, giữ a11y/reduced-motion.
- [ ] Test: smoke endpoints, approve persist→Done, SSE event, 401 thiếu token.

## Acceptance criteria

- UI load board thật từ API (smoke test gọi endpoints + 1 e2e nhẹ).
- Approve từ UI: `JUDGE_PASSED→DONE` persist, reload thấy ở cột Done — test.
- SSE phát event khi có transition — integration test.
- Endpoint đòi token hợp lệ; thiếu → 401 — test.

## Ánh xạ design

- Board / task detail / Approve human-only / không auto-merge: `TASK_HUB_DESIGN.md` §9.
- Timeline = comment + transition: `TASK_HUB_DESIGN.md` §8.
- `task.approve` human-only → DONE: `TASK_HUB_DESIGN.md` §6 + state machine §5.
- SSE + wiring tại chỗ (`node:http`): `IMPLEMENTATION_PLAN.md` §3, §9.3.

## Phụ thuộc & rủi ro

- **Phụ thuộc:** P5 (API/service), prototype UI đã có. Song song được với P6 (`IMPLEMENTATION_PLAN.md` §6).
- **Rủi ro:** giữ a11y AA + reduced-motion từ prototype khi wiring data sống (`IMPLEMENTATION_PLAN.md` §5 P7).
