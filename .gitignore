node_modules/
dist/
coverage/
logs/
*.db
.env
.claude/worktree/

# .ai/ workflow tooling is tracked; instance data (state/evidence/reports)
# is excluded via .ai/.gitignore.
.ai/

# Playwright
test-results/

# MCP registration — chứa bearer token thật, không bao giờ commit
.mcp.json

# SQLite sidecar (WAL mode) — *.db đã ignore nhưng không cover -shm/-wal
*.db-shm
*.db-wal

# macOS Finder metadata
.DS_Store
